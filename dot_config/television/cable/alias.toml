# Browse aliases loaded by zsh.
[metadata]
name = "alias"
description = "Browse shell aliases"
requirements = ["zsh"]

[source]
command = "zsh -f -c 'source ~/.zsh_aliases; alias -L' 2>/dev/null | sort"
