# Read-only Kubernetes context browser for Television.
[metadata]
name = "k8s-contexts"
description = "List and preview Kubernetes contexts"
requirements = ["kubectl"]

[source]
command = "kubectl config get-contexts -o name"

[preview]
command = "kubectl config view --minify --context='{}' -o yaml"

[ui]
layout = "portrait"
