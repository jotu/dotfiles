{{- if eq .chezmoi.os "darwin" }}
# Managed macOS Voxtype defaults. Model files stay outside Chezmoi.
engine = "whisper"

[hotkey]
key = "RIGHTALT"
mode = "push_to_talk"

[whisper]
model = "small.en"
language = "en"

[output]
mode = "type"
fallback_to_clipboard = true
{{- end }}
