---
description: Show available workflow commands and when to use them
agent: plan
---

Use skill tool to load superpowers-tidy-tdd-workflow.

Explain when to use each command:
- /route-work
- /brainstorm
- /write-plan
- /tidy-tdd-setup
- /execute-plan
- /verify-work
- /review-work

Also show the recommended sequence for non-trivial tasks.

Explain that `/route-work` is the best entrypoint when the right role or workflow path is unclear.
