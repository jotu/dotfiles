---
description: Route work to the right platform superteam role
agent: orchestrator
---

Use the platform superteam roles to classify and route this task.

Return:
1. Primary agent to own the work
2. Optional secondary or special-use agents to involve only if materially useful
3. Why this routing is the smallest effective setup
4. Recommended next command: /brainstorm, /write-plan, /tidy-tdd-setup, /execute-plan, /verify-work, or /review-work

Keep it concise.
If the request is materially unclear, ask 1-2 clarifying questions before routing.
Prefer one primary agent.
Prefer core roles by default.
Only involve secondary or special-use roles when they materially improve the outcome.
Do not create a large agent set by default.
