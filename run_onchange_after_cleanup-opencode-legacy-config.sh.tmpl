#!/bin/bash

set -eu

# Remove the legacy basename after the managed config migrated to oh-my-openagent.
rm -f "$HOME/.config/opencode/oh-my-opencode.json" "$HOME/.config/opencode/oh-my-opencode.json".bak*
