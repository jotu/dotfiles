#!/bin/bash

set -eu

# Remove the renamed upstream basename; this repo still renders the legacy-compatible file.
rm -f "$HOME/.config/opencode/oh-my-openagent.json" "$HOME/.config/opencode/oh-my-openagent.json".bak*
